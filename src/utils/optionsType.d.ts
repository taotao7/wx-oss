type options = {
  region: string;
  internal: Boolean;
  secure: Boolean;
  timeout: Number;
  bucket: String;
  endpoint: String;
  cname: Boolean;
  isRequestPay: Boolean;
  sldEnable: Boolean;
  headerEncoding: String;
  refreshSTSToken: String;
  refreshSTSTokenInterval: Number;
  retryMax: Number;
};
